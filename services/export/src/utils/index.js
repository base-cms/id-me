const sendFailure = require('./send-failure');
const sendSuccess = require('./send-sucess');
const streamResults = require('./stream-results');

module.exports = {
  sendFailure,
  sendSuccess,
  streamResults,
};
