<BsModal @show={{isOpen}} @onHidden={{action "clear"}} as |modal|>

  <form {{action "createAccessLevel" on="submit"}}>
    {{#modal.header}}
      <h5 class="modal-title">Create an access level</h5>
      <button
        type="button"
        class="close"
        aria-label="Close"
        {{action modal.actions.hide}}
      >
        {{entypo-icon "cross"}}
      </button>
    {{/modal.header}}

    {{#modal.body}}
      <div class="row">
        <div class="col">
          <div class="form-group">
            <label for="name">Name</label>
            {{input
              type="text"
              autofocus=true
              required=true
              class="form-control"
              value=name
            }}
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col">
          <div class="form-group">
            <label for="description">Description</label>
            {{input
              type="text"
              autofocus=true
              class="form-control"
              value=description
            }}
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col">
          {{#if (eq canSubmit false)}}
            <small class="d-block text-info mt-3">In order to save, {{reasonForPreventSubmit}}</small>
          {{/if}}
        </div>
      </div>
    {{/modal.body}}

    {{#modal.footer}}
      {{form-elements/submit-button
        class="btn btn-success"
        disabled=isSubmitDisabled
        isSaving=isActionRunning
      }}
    {{/modal.footer}}
    {{progress-bar show=isActionRunning}}
  </form>
</BsModal>
