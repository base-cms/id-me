<FormElements::ActionButton
  @type={{this.type}}
  @label={{this.label}}
  @disabled={{this.banned}}
  @icon={{this.icon}}
  @mustConfirm={{true}}
  @isLoading={{this.isLoading}}
  @onClick={{action this.onClick this.approved}}
/>
