<FormElements::ActionButton
  @type={{this.type}}
  @label={{this.label}}
  @icon="archive"
  @mustConfirm={{true}}
  @isLoading={{this.isLoading}}
  @onClick={{action this.onClick this.archived}}
/>
