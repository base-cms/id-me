<FormElements::ActionButton
  @type={{this.type}}
  @label={{this.label}}
  @disabled={{this.banned}}
  @icon="flag"
  @mustConfirm={{true}}
  @isLoading={{this.isLoading}}
  @onClick={{action this.onClick this.flagged}}
/>
