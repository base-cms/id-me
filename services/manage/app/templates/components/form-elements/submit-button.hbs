{{#form-elements/button
  id=id
  class=class
  type="submit"
  disabled=_disabled
}}
  {{#if isSaving}}
    {{entypo-icon "hour-glass" class="d-inline-block spin"}} Saving...
  {{else}}
    {{entypo-icon "save"}} Save
  {{/if}}
{{/form-elements/button}}
