<div class="card-body">
  <div class="row">
    <div class="col-md-2 text-center">
      <img src={{organization.photoURL}} alt={{organization.name}} class="icon icon-lg mr-4">
    </div>
    <div class="col">
      {{!-- <button title="Configure organization" class="float-right btn btn-sm btn-link">
        {{entypo-icon "cog"}}
      </button> --}}
      <h1>{{organization.name}}</h1>
      <small class="text-muted">
        {{organization.description}}
      </small>
    </div>
  </div>
</div>
