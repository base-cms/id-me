<h1 class="mb-4">
  <AppUser::Icon /> Users
  <span class="text-muted">({{model.totalCount}})</span>
</h1>

<div class="row">
  {{#each model.edges as |edge|}}
    <AppUser::Card @user={{edge.node}} />
  {{else}}
    <div class="col">
      <p>No users have been created.</p>
    </div>
  {{/each}}
</div>

<aside class="footer-controls left">
  <Button::Create @routeName="manage.orgs.view.apps.view.users.create" @title="Create New User" />
</aside>

{{outlet}}
